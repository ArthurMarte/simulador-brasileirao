[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["migrations"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "DJ"]

[tool.coverage.run]
omit = ["*/migrations/*", "*/tests/*", "config/wsgi.py", "manage.py"]
