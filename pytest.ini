[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
addopts =
    --cov=apps
    --cov-report=term-missing
    --cov-report=xml
    --cov-fail-under=80
    -ra
testpaths = apps
